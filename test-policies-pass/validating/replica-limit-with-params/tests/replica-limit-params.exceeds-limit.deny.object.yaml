apiVersion: apps/v1
kind: Deployment
metadata:
  name: large-deployment
spec:
  replicas: 10
  selector:
    matchLabels:
      app: test
  template:
    metadata:
      labels:
        app: test
    spec:
      containers:
      - name: nginx
        image: nginx

